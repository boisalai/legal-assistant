# ==============================================
# CONFIGURATION BACKEND - NOTARY ASSISTANT
# ==============================================
# Copiez ce fichier vers `.env` et remplissez les valeurs réelles
# ⚠️  NE JAMAIS commiter le fichier `.env` dans git !

# ----- API Configuration -----
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true
DEBUG=true

# ----- Security -----
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ----- Database (SurrealDB) -----
SURREAL_URL=ws://localhost:8001/rpc
SURREAL_NAMESPACE=notary
SURREAL_DATABASE=notary_db
SURREAL_USERNAME=root
SURREAL_PASSWORD=root

# ----- File Storage -----
UPLOAD_DIR=./data/uploads
MAX_UPLOAD_SIZE_MB=50

# ----- LLM Configuration -----
# Choisir un provider LLM parmi: mlx, huggingface, anthropic, ollama
LLM_PROVIDER=mlx

# ===== MLX (Apple Silicon - Local) =====
# Rapide sur M1/M2/M3, nécessite macOS
MLX_MODEL_PATH=mlx-community/Phi-3-mini-4k-instruct-4bit
# Autres modèles MLX:
# - mlx-community/Nous-Hermes-2-Mistral-7B-DPO-4bit (plus puissant, ~4GB)
# - mlx-community/Meta-Llama-3-8B-Instruct-4bit (très bon, ~5GB)

# ===== Hugging Face Transformers (Local) =====
# Fonctionne sur tous les OS, nécessite PyTorch
HF_MODEL_NAME=mistralai/Mistral-7B-Instruct-v0.2
HF_DEVICE=auto  # Options: auto, mps (M1), cuda (NVIDIA), cpu
HF_LOAD_IN_8BIT=false  # Charger en 8-bit (économise 50% RAM)
HF_LOAD_IN_4BIT=false  # Charger en 4-bit (économise 75% RAM)
# Autres modèles HF:
# - meta-llama/Meta-Llama-3-8B-Instruct (excellent)
# - microsoft/Phi-3-mini-4k-instruct (petit et rapide)
# - google/gemma-7b-it (bon sur les instructions)

# ===== Anthropic Claude (Cloud API) =====
# ⭐ Meilleur choix pour documents juridiques français
# Obtenir une clé: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Modèles recommandés (2025):
# - claude-sonnet-4-5-20250929 (⭐ RECOMMANDÉ - Excellent qualité/prix)
# - claude-opus-4-20250514 (Meilleur mais cher)
# - claude-sonnet-4-20250514 (Alternative sonnet)
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# ===== Ollama (Local - Cross-platform) =====
# Simple à utiliser, fonctionne sur tous les OS
# Télécharger depuis: https://ollama.ai
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral
# Autres modèles Ollama (installer avec `ollama pull <nom>`):
# - mistral (excellent rapport qualité/performance)
# - llama3 (très performant)
# - phi3 (petit et rapide)
# - gemma:7b (bon sur les instructions)

# ----- Agno Configuration -----
AGNO_LOG_LEVEL=INFO
AGNO_STORAGE_PATH=./data/agno_state
